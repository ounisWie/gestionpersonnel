{% extends 'base.html.twig' %}

{% block title %}Personne index{% endblock %}

{% block body %}
    <br><br>
<div class="content-wrapper" style="  align: center;">


    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h5 class="m-0 text-dark"><span style="margin-left: 115px;" >Relations</span> </h5>
                </div>

            </div>
        </div>
    </div><br>




    <section class="content">
        <div class="container-fluid">
            <div class="container">

            <table id="datatable" class="table table-striped table-bordered mt-2" style="width:100%">
        <thead>
        <tr>
            <th>#</th>
            <th>Nom</th>
            <th>Relation</th>

            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for personne in personnes %}
            <tr>
                <td>{{ personne.id }}</td>
                <td>{{ personne.nom }}</td>
                <td>{{ personne.prenom }}</td>

                <td>
                    <a class="btn btn-outline-secondary" href="{{ path('personne_edit', {'id': personne.id}) }}"><i class="far fa-edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12"class="table-danger text-center text-danger">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            <button class="btn btn-outline-secondary" href="{{ path('personne_new') }}">Create new</button>

            </div></div></section></div>
{% endblock %}
